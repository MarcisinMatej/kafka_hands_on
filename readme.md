# Kafka Ordering mini app


# Components

### Kafka

For local kafka we use docker-compose from this page https://hub.docker.com/r/wurstmeister/kafka/


```bash
# start in detached mode
docker-compose up -d

# kill and remove, so we can reuse the name
docker-compose down
```